{
  "name": "08-crud-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "json-server server/db.json --port 5040"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.3",
    "vite": "^6.1.0"
  }
}
